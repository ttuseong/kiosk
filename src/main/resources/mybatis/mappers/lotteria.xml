<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lotteria">

	<select id="categoryList" resultType="com.javaex.vo.LotteriaVo">
		<![CDATA[
			select categoryNo, categoryName from category where storeNo=2 order by categoryNo asc
		]]>
	</select>
	
	<select id="menuList" resultType="com.javaex.vo.LotteriaVo">
	<![CDATA[
		select sm.setNo setNo, 
			   sm.categoryNo categoryNo, 
			   sm.setName setName, 
			   sm.setPrice setPrice, 
			   sm.setImg setImg, 
			   sm.setDesc setDesc, 
			   c.storeNo storeNo 
		from setMenu sm, 
			 category c 
		where sm.categoryNo= c.categoryNo 
		and c.storeNo=2
	]]>
	</select>
		
	<select id="cateNo4" resultType="com.javaex.vo.LotteriaVo">
		<![CDATA[
			select menuNo, categoryNo, menuName, menuDesc, menuPrice, menuImg, isSpecial from Menu where categoryNo = 4 and isSpecial=1
		]]>
	</select>
	
</mapper>